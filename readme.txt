behavior: Student should be a class that contains a school student. It holds a student ID, First Name, Last Name, and list of assignment grades. It should hold the following functions:
	__init__(id, first, last) initializes the Student with the given data
	-add_grade(percent) appends a new grade to the grade list
	-calculate_grade() returns the student's grade; a (rounded down) average of the grade list
	-print_transcript() outputs important data to the screen: in the format "Last, First : ID; Grade%" e.g. "Junwaowam, Patpawat : 8110738; 71%"
concepts: application of class, basic application of list, basic application of algorithm, text formatting
library functions: use floor() to round a decimal down to an integer